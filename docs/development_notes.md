# Development Notes
- Backend is FastAPI + pandas/numpy with modular engines for data, orderbook, backtest, execution, analytics, sandbox.
- Frontend uses React + Zustand + Vite with terminal-themed density-first UI.
- Persistent metadata and run summaries are stored in SQLite.
